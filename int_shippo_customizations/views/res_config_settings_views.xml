<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.int.shippo</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="stock.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//app[@name='stock']" position="inside">
                <block title="Shippo" name="int_shippo_settings">
                    <setting string="API token" help="Paste a Shippo test token first (shippo_test_…). Switch to a live token when you are ready to buy real labels.">
                        <field name="int_shippo_api_token" password="True"/>
                    </setting>
                    <setting string="Test mode" help="Reminder only. Shippo decides test vs live from the token, not this checkbox.">
                        <field name="int_shippo_test_mode"/>
                    </setting>
                    <setting string="Boxes" help="Carton sizes sent to Shippo. These are not Inventory Packages.">
                        <button name="%(int_shippo_customizations.action_int_shippo_box)d" type="action"
                                string="Shippo Boxes" class="btn-link"/>
                    </setting>
                </block>
            </xpath>
        </field>
    </record>
</odoo>
